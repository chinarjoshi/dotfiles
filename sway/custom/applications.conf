# Startup applications
exec autotiling-rs
exec blueman-applet
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
exec libinput-gestures-setup start
exec mako
exec nm-applet --indicator
exec udiskie
# exec wluma
exec wlsunset -l 34.0 -L -84.2
exec ydotoold
